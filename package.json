{
  "name": "portflow-ai",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:spec-kit": "node --test scripts/spec-kit/*.test.mjs",
    "typecheck": "turbo run typecheck",
    "spec:workflow": "node scripts/spec-kit/workflow.mjs",
    "project:kanban": "node scripts/spec-kit/configure-project-kanban.mjs",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "check": "biome check .",
    "check:fix": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "turbo": "^2.10.4"
  }
}
