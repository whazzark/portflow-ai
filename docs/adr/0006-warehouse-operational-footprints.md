# Represent warehouses with operational footprint polygons

Warehouses are represented by geographic polygons covering their operational footprint, including truck unloading positions, rather than by a single GPS point. This supports a faithful map of each warehouse and lets every warehouse door remain an exact unloading point within or on that footprint; the warehouse display center is derived from the polygon.
