# Store immutable report snapshots with source data

We will generate each report snapshot as an immutable PDF together with the structured data used to produce it. The PDF is the transmissible document, while the stored structured payload preserves the validated rotations, totals, coverage, and metadata as they existed at generation time, so later corrections do not change the audit record of previously sent reports.
