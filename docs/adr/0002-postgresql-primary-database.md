# Use PostgreSQL as the primary database

We will use PostgreSQL as the primary database for the MVP. The discharge domain needs relational integrity, reporting queries, traceable report snapshots, and aggregations across shifts, customers, product lots, warehouses, and rotations, making PostgreSQL a better fit than a lightweight development-only database.
