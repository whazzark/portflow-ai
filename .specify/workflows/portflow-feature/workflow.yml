schema_version: "1.0"

workflow:
  id: "portflow-feature"
  name: "Portflow Full Spec-Driven Delivery"
  version: "2.0.0"
  author: "Portflow"
  description: "Phase contract used by the issue-driven terminal orchestrator for new features"

requires:
  speckit_version: ">=0.14.2"
  integrations:
    any: ["codex"]

inputs:
  spec:
    type: string
    required: true
    prompt: "Low-level compatibility input; daily operation uses pnpm spec:workflow with a GitHub issue"

steps:
  - id: specify
    command: speckit.specify
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: clarify
    command: speckit.clarify
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: review-spec
    type: gate
    message: "Review spec.md and resolve every product decision before planning."
    options: [approve, reject]
    on_reject: retry

  - id: plan
    command: speckit.plan
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: checklist
    command: speckit.checklist
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: review-plan
    type: gate
    message: "Review plan.md and checklist before implementation tasks are generated."
    options: [approve, reject]
    on_reject: retry

  - id: tasks
    command: speckit.tasks
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: analyze
    command: speckit.analyze
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: implement
    command: speckit.implement
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: fast-checks
    type: shell
    run: "pnpm check && pnpm typecheck && pnpm test && pnpm test:spec-kit"

  - id: converge
    command: speckit.converge
    integration: codex
    input:
      args: "{{ inputs.spec }}"

  - id: final-review
    type: gate
    message: "Review the implementation, checks, convergence report, and Draft PR before marking it ready."
    options: [approve, reject]
    on_reject: retry
